gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java

# Signing secrets — ҲЕҶ ГОҲ ба source control гузошта нашаванд (Google Play prod).
# Локалӣ дар android/key.properties + android/app/tajikshop-release.jks;
# дар CI аз GitHub Secrets (KEYSTORE_BASE64, STORE_PASSWORD, KEY_PASSWORD, KEY_ALIAS).
key.properties
**/*.keystore
**/*.jks
